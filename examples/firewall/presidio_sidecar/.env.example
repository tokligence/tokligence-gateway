# Presidio Sidecar Configuration
# Copy this file to .env and adjust values

# Server configuration
PRESIDIO_HOST=0.0.0.0
PRESIDIO_PORT=7317  # Default port (avoids conflicts with common services)

# Performance tuning
# Set this to your CPU core count for optimal performance
# Recommendation: (CPU cores) × 2
PRESIDIO_WORKERS=4

# Logging
PRESIDIO_LOG_LEVEL=info  # debug, info, warning, error

# Development mode (disable in production)
PRESIDIO_RELOAD=false

# ==============================================================================
# NER Engine Configuration (DEFAULT: XLM-RoBERTa)
# ==============================================================================
# Engine selection: xlmr (default, best for Chinese) | spacy (faster startup)
PRESIDIO_NER_ENGINE=xlmr

# XLM-RoBERTa configuration (when PRESIDIO_NER_ENGINE=xlmr)
XLMR_NER_MODEL=hrl           # Options: hrl (10 langs, recommended) | wikiann (20 langs)
XLMR_NER_DEVICE=cpu          # Options: cpu | gpu | gpu:0 | gpu:1 (gpu recommended for production)

# spaCy configuration (when PRESIDIO_NER_ENGINE=spacy)
# PRESIDIO_SPACY_MODEL=xx_ent_wiki_sm  # Multilingual model
# PRESIDIO_MULTILINGUAL=true

# Optional: Redis caching (requires redis-py)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_ENABLE=false
