; Local Token Exchange endpoint for dev workflows.
base_url=http://localhost:8080
; Dev account identity; typically a test inbox.
email=dev@example.com
; Name shown when listing the gateway locally.
display_name=Dev Gateway
; Toggle provider publishing for local testing (true/false).
enable_provider=false
; Optional service name to push to marketplace during dev.
publish_name=
; Label for the model class advertised to consumers.
model_family=
; Dev-specific log file location.
log_file=logs/dev-cli.log
